/* Requires the Docker Pipeline plugin */
pipeline {
    agent { label 'master' }
    stages {
        stage ('FullPageOS') {
            build job: 'FullPageOS'
        }
        stage ('FullPageOS non accelration') {
            build job: 'FullPageOS - noacc'
        }
    }
}
